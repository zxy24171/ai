# AI 对话应用 — 产品与技术规格说明书

> 版本：v1.0  
> 日期：2026-06-12  
> 状态：草案 / 待实现

---

## 目录

1. [产品概述](#1-产品概述)
2. [架构总览](#2-架构总览)
3. [用户故事](#3-用户故事)
4. [交互流程详解](#4-交互流程详解)
5. [技术方案](#5-技术方案)
6. [端云协同与成本控制](#6-端云协同与成本控制)
7. [数据流与状态管理](#7-数据流与状态管理)
8. [UI/UX 设计原则](#8-UIUX-设计原则)
9. [里程碑与交付物](#9-里程碑与交付物)
10. [附录 A：成本控制技术清单](#10-附录-a成本控制技术清单)

---

## 1. 产品概述

### 1.1 产品定位

一款**浏览器端运行的实时 AI 对话应用**，用户打开摄像头与麦克风后，AI 能够：
- 看到摄像头画面中的内容（视觉理解）
- 听到用户说的话（语音识别）
- 以自然语音回应用户（语音合成）
- 在合适的时候做出视觉相关的反馈（例如"我看到你面前放着一杯咖啡"）

### 1.2 核心价值

| 维度 | 描述 |
|------|------|
| 自然度 | 多模态交互，接近人与人的面对面交流 |
| 即时性 | 端侧预处理 + 云侧推理，低延迟 |
| 经济性 | 按需调度、本地缓存、语义降频，控制 API 成本 |
| 隐私友好 | 端侧做脱敏、裁剪，敏感数据不上云 |

---

## 2. 架构总览

`
┌────────────────────────────────────────────────────────────┐
│                    浏览器 (用户端)                          │
│                                                             │
│  ┌──────────┐   ┌──────────┐   ┌────────────────────────┐ │
│  │ 摄像头    │──▶│ 视频帧    │──▶│ 帧筛选 & 压缩          │ │
│  │ (WebRTC) │   │ 采集器    │   │ (运动检测 + JPEG)      │ │
│  └──────────┘   └──────────┘   └───────────┬────────────┘ │
│                                            │               │
│  ┌──────────┐   ┌──────────┐   ┌───────────▼────────────┐ │
│  │ 麦克风    │──▶│ 音频流    │──▶│ VAD + 语音端点检测     │ │
│  │ (WebRTC) │   │ 采集器    │   │ (Silero VAD / 本地)    │ │
│  └──────────┘   └──────────┘   └───────────┬────────────┘ │
│                                            │               │
│  ┌─────────────────────────────────────────▼───────────┐  │
│  │            多模态上下文管理器                          │  │
│  │  (文本 + 图片帧 + 系统提示 + 对话历史)                 │  │
│  └──────────────────────────┬──────────────────────────┘  │
│                             │                              │
│  ┌──────────────────────────▼──────────────────────────┐  │
│  │          API 代理层 (请求组装 / 降频 / 缓存)          │  │
│  └──────────────────────────┬──────────────────────────┘  │
└─────────────────────────────┼────────────────────────────┘
                              │ HTTPS / WebSocket
┌─────────────────────────────┼────────────────────────────┐
│                    云侧                                    │
│  ┌──────────────────────────▼──────────────────────────┐  │
│  │   GPT-4o / GPT-4o-mini (多模态推理)                   │  │
│  │   + TTS (语音合成)                                    │  │
│  │   + STT (语音转文字, 可选的)                           │  │
│  └─────────────────────────────────────────────────────┘  │
└────────────────────────────────────────────────────────────┘
`

### 2.1 分层职责

| 层次 | 组件 | 职责 |
|------|------|------|
| **采集层** | 摄像头 / 麦克风 | 通过 getUserMedia 获取原始音视频流 |
| **预处理层** | 帧筛选器、VAD | 运动检测剪枝、语音活动检测、帧压缩 |
| **上下文层** | 多模态上下文管理器 | 维护对话历史、帧序列、系统指令组装 |
| **代理层** | API 代理 | 请求频率控制、Token 预算管理、缓存命中 |
| **推理层** | 云端 LLM + TTS | 多模态理解 + 自然语言生成 + 语音输出 |

---

## 3. 用户故事

### 3.1 规划的用户故事

| ID | 故事 | 优先级 | 预估工作量 |
|----|------|--------|-----------|
| US-01 | 作为用户，我希望打开应用后能一键授权摄像头和麦克风，以便快速开始对话 | P0 | 1d |
| US-02 | 作为用户，我希望 AI 能实时看到我的摄像头画面，并对场景做出合理描述或回应 | P0 | 3d |
| US-03 | 作为用户，我希望用语音与 AI 对话，而不需要打字 | P0 | 2d |
| US-04 | 作为用户，我希望 AI 用自然语音回答我，而不是纯文字 | P0 | 2d |
| US-05 | 作为用户，我希望对话上下文保持连贯，AI 记得之前说过什么 | P0 | 2d |
| US-06 | 作为用户，我希望应用不要因为频繁调用 API 而产生高昂费用，但仍保持体验流畅 | P1 | 3d |
| US-07 | 作为用户，我可以在设置中调节语音速度、音量、语言 | P2 | 1d |
| US-08 | 作为用户，我可以看到对话的文字历史记录，方便回溯 | P1 | 1d |
| US-09 | 作为用户，我可以随时静音或关闭摄像头 | P1 | 0.5d |
| US-10 | 作为用户，我可以在弱网环境下继续对话（降级策略） | P2 | 2d |

### 3.2 最终实现计划

> 第一阶段 (MVP) 实现以下用户故事：

| 阶段 | 包含的故事 | 目标 |
|------|-----------|------|
| **MVP** | US-01, US-02, US-03, US-04, US-05, US-08, US-09 | 核心对话体验可用 |
| **V1.1** | US-06 (基础成本控制) | 增加降频、帧压缩 |
| **V1.2** | US-07, US-10 | 设置项 & 弱网降级 |

---

## 4. 交互流程详解

### 4.1 一次典型交互的完整流程

`
用户说话 ──▶   VAD 检测到语音开始
                │
                ▼
         音频持续采集 ──▶  VAD 检测到语音结束 ( endpoint )
                │
                ▼
         发送音频到 STT (Whisper API) 或直接发送多模态请求
                │
                ▼
         当前最新摄像头帧 (1-2帧) 编码为 base64 JPEG
                │
                ▼
         组装 Prompt: 系统指令 + 历史消息 + 用户语音文本 + 图像帧
                │
                ▼
         调用 GPT-4o (多模态) ──▶ 流式返回文本
                │
                ▼
         文本送入 TTS ──▶ 播放语音
                │
                ▼
         将本轮消息存入历史
`

### 4.2 关键交互节点设计

#### 4.2.1 摄像头帧采集策略

- 采集频率：**5 FPS**（每 200ms 取一帧），降低云侧 Token 消耗
- 运动检测：计算相邻帧的像素差异，若差异 < 阈值则跳过上传
- 分辨率控制：默认 640×480，设置中可调（320×240 / 640×480 / 1280×720）
- 编码格式：JPEG quality 60，平衡画质与大小

#### 4.2.2 语音采集策略

- 使用 AudioContext + MediaStream API
- 采样率：16kHz（Whisper 最优输入）
- VAD：使用 Silero VAD 模型（ONNX Runtime Web 版）进行本地端点检测
- 静音阈值：自动降噪后，能量 < -35dBFS 判定为静音
- **超时保护**：用户停顿 > 30s 视为意图结束，自动触发一次主动问候

#### 4.2.3 多模态 Prompt 模板

`
System:
你是一个实时 AI 助手。你正在通过摄像头观察用户的实时画面，
并可以通过语音与用户对话。
- 请基于当前画面和对话历史给出自然、简洁的回答。
- 如果画面中有值得注意的事物（物体、动作、状态变化），可以主动提及。
- 请用中文回答，保持对话自然流畅。
- 如果画面没有明显变化，不必每一轮都重复描述画面。

History: [对话历史]
User: [语音转文字结果]
Image: [base64 JPEG 帧]
`

---

## 5. 技术方案

### 5.1 技术栈选型

| 领域 | 选择 | 理由 |
|------|------|------|
| 前端框架 | **React 18 + Vite** | 生态成熟、HMR 快、社区活跃 |
| 语言 | **TypeScript** | 类型安全，减少运行时错误 |
| 摄像头/麦克风 | **WebRTC getUserMedia** | 浏览器原生 API，零依赖 |
| 多模态推理 | **OpenAI GPT-4o** | 原生支持图片 + 文本输入，理解力强 |
| 语音转文字 | **OpenAI Whisper API** 或 **GPT-4o-audio-preview** | 准确率高，支持中文 |
| 文字转语音 | **OpenAI TTS API** (tts-1 / tts-1-hd) | 自然度好，延迟可接受 |
| 流式播放 | **Web Audio API + AudioContext** | 低延迟音频播放 |
| VAD | **Silero VAD** (onnxruntime-web) | 本地运行，隐私友好 |
| 样式 | **Tailwind CSS** | 快速迭代，一致的 UI |

### 5.2 核心 API 选型对比

#### 多模态方案

| 方案 | 延迟 | 视觉理解 | 成本 | 推荐度 |
|------|------|---------|------|--------|
| **GPT-4o (vision + text)** | 中 | ★★★★★ | 中高 | ★★★★★ 推荐 |
| GPT-4o-mini (vision) | 低 | ★★★★ | 低 | ★★★★ 备选 |
| Gemini 1.5 Flash | 低 | ★★★★ | 低 | ★★★ 备选 |
| 本地多模态模型 | 高 | ★★★ | 免费 | ★★ 不适合 MVP |

**结论**：MVP 使用 GPT-4o 确保理解质量，后续可降级到 GPT-4o-mini 做成本优化。

#### 语音方案

| 方案 | 延迟 | 音质 | 成本 | 推荐度 |
|------|------|-----|------|--------|
| **OpenAI TTS (tts-1)** | 低 | ★★★★ | .015/1K chars | ★★★★★ 推荐 |
| OpenAI TTS (tts-1-hd) | 中 | ★★★★★ | .030/1K chars | ★★★ 高音质场景 |
| Web Speech API (SSML) | 零 | ★★ | 免费 | ★★ 降级备用 |

### 5.3 关键数据结构

`	ypescript
// 消息模型
interface Message {
  id: string;
  role: 'user' | 'assistant' | 'system';
  content: string;
  timestamp: number;
  // 用户消息附带信息
  images?: string[];       // base64 帧 (仅 user 消息)
  audioUrl?: string;       // 用户语音 blob URL
  // Assistant 消息附带信息
  ttsUrl?: string;         // 语音回复 blob URL
  tokenCost?: number;      // 本次消耗
}

// 对话会话
interface Session {
  id: string;
  messages: Message[];
  createdAt: number;
  settings: SessionSettings;
  tokenUsage: { total: number; estimatedCost: number };
}

// 设置项
interface SessionSettings {
  cameraResolution: '320x240' | '640x480' | '1280x720';
  frameInterval: number;       // ms, 默认 200
  voiceSpeed: number;          // 0.5 - 2.0
  voiceModel: 'tts-1' | 'tts-1-hd';
  language: 'zh' | 'en' | 'auto';
  costSaveMode: 'off' | 'balanced' | 'aggressive';
}
`

### 5.4 帧筛选算法（伪代码）

`
function shouldSendFrame(prevFrame, currentFrame, threshold=0.05):
    if prevFrame is null: return true
    diff = pixelwiseDifference(prevFrame, currentFrame)
    normalizedDiff = diff / totalPixels
    return normalizedDiff > threshold

function selectBestFrame(frames[], maxFrames=2):
    // 从候选帧中选择差异最大的 2 帧
    sorted = frames.sortByDifference()  // 与前一发送帧比较
    return sorted.slice(0, maxFrames)
`

---

## 6. 端云协同与成本控制

本章节是产品成本控制的核心设计，分为"策略库"（所有想到的）和"实际采用"两个维度。

### 6.1 成本分析基线

以 GPT-4o 为基准，假设每次交互消耗：

| 项目 | 估算 |
|------|------|
| 系统提示 | ~300 tokens |
| 对话历史 | ~500 tokens/轮 × 10 轮 = ~5K tokens |
| 图像帧 | ~260 tokens/帧 (JPEG 640×480) × 2 帧 = ~520 tokens |
| 用户语音文本 | ~50 tokens |
| 模型输出 | ~200 tokens |
| **单次请求** | ~6,070 tokens ≈ .03 (GPT-4o) |
| 假设 50 次/天 × 30 天 | **~/月/用户** |

### 6.2 策略清单 —— 想到的 vs 采用的

| # | 策略名称 | 描述 | 想到 | 采用 |
|---|---------|------|------|------|
| C1 | **动态帧率控制** | 对话活跃时高频传帧，静默时降频或停传 | ✅ | ✅ |
| C2 | **运动检测剪枝** | 画面无明显变化时不传帧 | ✅ | ✅ |
| C3 | **图像压缩** | JPEG quality 可调，分辨率可降级 | ✅ | ✅ |
| C4 | **语义降频** | 每 N 秒内只传差异化帧；前帧已含足够信息则不重传 | ✅ | ✅ |
| C5 | **GPT-4o-mini 降级** | 简单对话使用 GPT-4o-mini (成本 ~1/20) | ✅ | ✅ |
| C6 | **对话历史剪枝** | 超出 max_tokens 时丢弃早期历史 | ✅ | ✅ |
| C7 | **图片缓存去重** | 连续帧相似度 > 阈值时不传输，仅标记"画面无变化" | ✅ | ✅ |
| C8 | **夜间/低活跃模式** | 检测到用户长时间无交互，自动暂停摄像头采集 | ✅ | ✅ |
| C9 | **本地 VAD 过滤** | 只有检测到有效语音才发起请求；环境噪声不触发 | ✅ | ✅ |
| C10 | **TTS 缓存** | 相同的回答文本不用重复调用 TTS | ✅ | ❌ |
| C11 | **流式 TTS 中断** | 用户打断时停止当前 TTS 播放，节省无用输出 | ✅ | ✅ |
| C12 | **请求批处理** | 多条累积后一次发送 | ✅ | ❌ |
| C13 | **端侧模型兜底** | 离线时用本地模型做简单回应（如打招呼） | ✅ | ❌ |
| C14 | **设置化成本模式** | 提供 off / balanced / aggressive 三级用户可选 | ✅ | ✅ |
| C15 | **Token 用量仪表盘** | 显示实时 Token 消耗和估算费用 | ✅ | ✅ |
| C16 | **沉默自动休眠** | 连续 N 分钟无对话，释放摄像头/麦克风资源 | ✅ | ✅ |
| C17 | **端侧帧降噪去重** | 在发送前对帧做感知哈希去重 | ✅ | ✅ |

### 6.3 三级成本模式定义

| 模式 | 帧率 | 分辨率 | 模型 | VAD 阈值 | 历史轮数 | 预估小时成本 |
|------|------|--------|------|---------|---------|-------------|
| **off** (无优化) | 5 FPS | 640×480 | GPT-4o | 低 | 20 轮 | ~.80 |
| **balanced** (默认) | 2 FPS (运动检测) | 480×360 | GPT-4o + 降级路由 | 中 | 10 轮 | ~.40 |
| **aggressive** (省钱) | 1 FPS (仅明显变化) | 320×240 | GPT-4o-mini | 高 | 6 轮 | ~.04 |

### 6.4 降级路由决策树

`
收到用户请求
    │
    ├── 判断对话复杂度
    │   ├── 简单指令 (问候、时间、天气) ──▶ GPT-4o-mini
    │   ├── 需要视觉理解的 ──▶ GPT-4o
    │   └── 复杂推理或多轮 ──▶ GPT-4o
    │
    ├── 判断当前帧变化
    │   ├── 无变化 ──▶ 不传帧，文本 + "画面无变化" 标记
    │   └── 有变化 ──▶ 传 1-2 帧
    │
    └── 判断网络状况
        ├── 弱网 ──▶ 降低分辨率 + JPEG quality
        └── 离线 ──▶ 显示"离线"提示，端侧做简单回应
`

### 6.5 Token 预算管理

`	ypescript
interface TokenBudget {
  maxContextTokens: number;      // 4000 (balanced) / 2000 (aggressive)
  maxFramesPerRequest: number;   // 2 帧
  maxHistoryRounds: number;      // 10 轮
  frameTokenQuota: number;       // 每帧预算 260 tokens
}
`

当上下文超出预算时：
1. 优先丢弃最旧的对话轮次
2. 保留系统指令
3. 保留最近 2 轮的图像帧
4. 如果仍超出，压缩图像质量

---

## 7. 数据流与状态管理

### 7.1 React 状态结构

`	ypescript
// 全局状态
interface AppState {
  // 设备状态
  camera: { enabled: boolean; stream: MediaStream | null; error: string | null };
  microphone: { enabled: boolean; stream: MediaStream | null; error: string | null };
  
  // 对话状态
  session: Session;
  isProcessing: boolean;         // 正在等待 AI 回复
  isSpeaking: boolean;          // AI 正在播放语音
  
  // 帧管理
  framePipeline: {
    latestFrame: string | null;  // 最新 base64 帧
    lastSentFrame: string | null;
    frameHistory: string[];      // 最近 N 帧的缓冲区
  };
  
  // 音频管理
  audioPipeline: {
    isRecording: boolean;
    audioChunks: Blob[];
    ttsQueue: string[];          // TTS 播放队列
  };
  
  // 成本控制
  costMetrics: {
    totalTokens: number;
    totalCost: number;
    requestCount: number;
    currentMode: 'off' | 'balanced' | 'aggressive';
  };
  
  // 设置
  settings: SessionSettings;
}
`

### 7.2 主要数据流

`
用户语音 ──▶ VAD ──▶ [识别] ──▶ 文本 + 帧 ──▶ API ──▶ 回复文本 ──▶ TTS ──▶ 播放
                                      │                     │
                                      ▼                     ▼
                                 存入 Message[]         存入 Message[]
                                 更新 Token 统计        设置 isSpeaking = false
`

---

## 8. UI/UX 设计原则

### 8.1 页面布局

`
┌───────────────────────────────────────────────────┐
│  ┌──────────┐  ┌────────────────────────────────┐ │
│  │           │  │                                │ │
│  │  摄像头   │  │    对话消息列表                 │ │
│  │  预览     │  │    (滚动区域)                   │ │
│  │           │  │                                │ │
│  │           │  │                                │ │
│  └──────────┘  └────────────────────────────────┘ │
│  ┌────────────────────────────────────────────────┐ │
│  │  状态栏:  🎙️ 录制中  📷 已开启  💰 .03     │ │
│  │  [设置] [静音] [关摄像头] [成本模式]          │ │
│  └────────────────────────────────────────────────┘ │
└───────────────────────────────────────────────────┘
`

### 8.2 设计原则

1. **最小干扰**：对话过程中 UI 保持简洁，不以按钮或弹窗打断交互
2. **状态可见**：摄像头/麦克风状态始终可见，让用户有掌控感
3. **延迟反馈**：AI 思考时用温和的动画提示（如脉冲光点），而非冷冰冰的 loading
4. **成本透明**：Token 用量和估算费用实时显示，帮助用户形成成本意识
5. **渐进披露**：进阶设置（分辨率、帧率、模型选择）折叠在设置面板中

---

## 9. 里程碑与交付物

| 里程碑 | 产出物 | 交付时间 |
|--------|-------|---------|
| **M1: Spec 定稿** | 本文档 | Day 0 |
| **M2: 基础框架** | React 项目搭建、设备权限获取、摄像头/麦克风预览 | Day 1 |
| **M3: 多模态对话** | GPT-4o 集成、帧采集与发送、对话历史管理 | Day 3 |
| **M4: 语音交互** | VAD 集成、STT/Whisper、TTS 播放、语音打断 | Day 5 |
| **M5: 成本控制** | 动态帧率、降级路由、Token 预算管理、仪表盘 | Day 7 |
| **M6: 体验打磨** | 弱网降级、设置面板、错误恢复、UI 完善 | Day 9 |
| **M7: 设计文档交付** | 最终版设计文档（含用户故事对比、成本策略对比） | Day 9 |

---

## 10. 附录 A：成本控制技术清单

### 完整清单对照表

| 策略 | 预期节省 | 实现难度 | 是否采用 | 原因 |
|------|---------|---------|---------|------|
| 动态帧率控制 | ~40% 帧相关成本 | 低 | ✅ | 核心策略，实现简单效果显著 |
| 运动检测剪枝 | ~30% 帧相关成本 | 中 | ✅ | 与动态帧率互补 |
| 图像压缩 | ~15% 帧相关 Token | 低 | ✅ | 几乎零成本实现 |
| 语义降频 | ~20% 帧相关成本 | 低 | ✅ | 避免冗余帧传输 |
| GPT-4o-mini 降级 | ~95% 推理成本 | 低 | ✅ | 成本杀手，简单对话效果足够 |
| 对话历史剪枝 | 防止 OOT，间接控制 | 低 | ✅ | 必要的安全措施 |
| 图片缓存去重 | 叠加至 ~25% | 中 | ✅ | 感知哈希实现不复杂 |
| 本地 VAD 过滤 | ~60% 无效请求 | 中 | ✅ | 核心策略 |
| TTS 缓存 | ~5% TTS 成本 | 中 | ❌ | 回复文本重复率低，收益有限 |
| 流式 TTS 中断 | 减少无用输出 | 中 | ✅ | 用户打断场景常见 |
| 请求批处理 | ~10% 网络开销 | 高 | ❌ | 与实时性冲突，不适合语音对话 |
| 端侧模型兜底 | 离线场景 100% | 高 | ❌ | MVP 范围外 |
| 沉默自动休眠 | ~50% 空闲资源 | 低 | ✅ | 用户离开场景省资源 |
| Token 用量仪表盘 | 行为引导 | 低 | ✅ | 让用户自我管理，间接降本 |

> **采用策略合计**：11 项  
> **预估综合节省**：在 balanced 模式下，相比无优化方案节省约 75-85% 的 API 成本。

---

*本文档为产品规格说明书，后续实现阶段将根据实际情况调整。*
